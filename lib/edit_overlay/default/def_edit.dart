import 'package:flutter/material.dart';

class Default_Creator extends StatelessWidget {
  const Default_Creator({super.key});

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
